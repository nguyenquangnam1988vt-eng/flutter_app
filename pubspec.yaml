name: driver_phone_monitor
description: Ứng dụng cảnh báo khi sử dụng điện thoại lái xe
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  sensors_plus: ^7.0.0
  geolocator: ^14.0.2
  permission_handler: ^10.4.0
  vibration: ^1.7.4
  flutter_local_notifications: ^13.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true